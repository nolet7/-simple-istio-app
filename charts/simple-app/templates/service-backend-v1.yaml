apiVersion: v1
kind: Service
metadata:
  name: backend-v1
  labels:
    app: backend
    version: v1
spec:
  type: {{ .Values.backendV1.service.type }}
  ports:
    - port: {{ .Values.backendV1.service.port }}
      targetPort: {{ .Values.backendV1.service.targetPort }}
  selector:
    app: backend
    version: v1
